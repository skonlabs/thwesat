// Legacy wallet chip — hidden in Phase 2A (subscription model replaces credit wallet).
// File kept so imports don't break; component renders nothing.
const WalletChip = () => null;
export default WalletChip;
